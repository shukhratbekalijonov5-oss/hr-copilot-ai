import { SettingsScreen } from "@/components/settings/SettingsScreen";

/** The same screen as the candidate's: nothing here is role-specific. */
export default function RecruiterSettingsScreen() {
  return <SettingsScreen />;
}
