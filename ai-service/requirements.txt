# --- API -------------------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
# 2.13.4, not 2.10.4: `google-genai` 2.18 requires a pydantic newer than the
# old pin, so the two could not resolve together. The venv this service
# actually runs on has been on 2.13.4 for some time — the pin had simply
# drifted from reality, and a container build was the first thing to resolve
# the file from scratch and notice.
pydantic==2.13.4
pydantic-settings==2.7.0
python-multipart==0.0.20

# --- Embeddings / reranking ------------------------------------------------
# torch is the inference engine for both the bi-encoder (embeddings) and the
# cross-encoder (reranker). CPU wheels are sufficient for local development.
torch==2.5.1
sentence-transformers==3.3.1

# --- Grounded generation ---------------------------------------------------
# Both are imported lazily inside their provider module; only the configured
# provider is ever loaded. `google-genai` is the current unified Google SDK —
# the older `google-generativeai` line is deprecated and is not used.
google-genai==2.18.1
anthropic==0.69.0

# --- Vector store ----------------------------------------------------------
qdrant-client==1.12.2

# --- Document parsing ------------------------------------------------------
# pdfminer.six is the primary PDF extractor (layout-aware: reconstructs word
# boundaries and column order from glyph positions); pypdf is the fallback
# text-layer reader and the magic-byte/corruption gate.
pdfminer.six==20260107
pypdf==5.1.0
python-docx==1.1.2

# --- Utilities -------------------------------------------------------------
httpx==0.28.1
