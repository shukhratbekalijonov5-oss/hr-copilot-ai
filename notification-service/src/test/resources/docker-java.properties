# The local Docker daemon (29.x) rejects API versions below 1.44 with a 400,
# and docker-java defaults to 1.32. Pin a version every modern daemon speaks.
# Project-scoped (test classpath) so nothing outside this build is affected.
api.version=1.44
